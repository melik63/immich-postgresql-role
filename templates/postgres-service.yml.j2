apiVersion: v1
kind: Service
metadata:
  name: {{ postgres_host }}
  namespace: {{ postgres_namespace }}
spec:
  selector:
    app: postgres
  ports:
    - protocol: TCP
      port: 5432
      targetPort: {{ postgres_port }}
  type: ClusterIP
